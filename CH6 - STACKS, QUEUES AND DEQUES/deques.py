# A double ended queue(deque). Pronounced deck. It supports insertion and 
# deletion at both the front and back of the queue. 
# It's a generalized version of the the stack and queue.

# d.add_first(e)... adds element e to the front of deque d
# d.add_last(e)... adds element e to the back of deque d
# d.delete_first()... removes and returns the first element 
# d. delete_last()... removes and returns the last element
# d.first()... returns first element.
# d.last()... returns last element.
